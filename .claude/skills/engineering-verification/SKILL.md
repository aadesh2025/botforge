---
name: engineering-verification
description: Senior-engineer verification and stabilization pass on an existing repository, plus a change-impact protocol for adding features safely. Discovers the project's real commands and architecture, establishes a baseline, separates regressions from pre-existing, environmental and flaky failures, verifies security boundaries, fixes only justified issues, and reports factually. Use to assess or stabilize a codebase, to check a recent change, or before implementing a non-trivial feature. Not for redesigns or unsolicited refactors.
---

# engineering-verification (Claude Code adapter)

This file is a pointer. The protocol is agent-neutral and lives in one place:

`skills/engineering-verification/SKILL.md` (relative to the repository root)

Read that file in full now and follow it. It links to `skills/engineering-verification/references/` (read the
one you need when you reach that step) and `skills/engineering-verification/templates/` (use them for the
baseline, architecture map, verification report and feature impact plan/report).

Do not treat this adapter as a summary of the protocol; it deliberately contains none of it. Keep its
`description` identical to the canonical `SKILL.md`.
